'use client'

import Navitem from '@components/Nav/Itemnav'
import Image from 'next/image'
import Link from 'next/link'
import Tonalbutton from '@Buttons/Tonal'
import { GithubJhan, LinkedinJhan, VercelHarkaysoft } from '@routes'

export default function Perfil({ slug }: { slug: string }) {
  const id = slug

  // if (!producto) {
  //   return notFound()
  // }

  return <div className='container mx-auto lg:px-16'>HOLA</div>
}
